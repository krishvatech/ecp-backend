"""Messaging app initialization.

The messaging app enables private 1‑to‑1 conversations between users.
It registers itself with Django via the MessagingConfig class.
"""

default_app_config = "messaging.apps.MessagingConfig"
